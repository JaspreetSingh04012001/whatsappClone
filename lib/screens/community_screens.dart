import 'package:flutter/material.dart';

class CommunityScreens extends StatelessWidget {
  const CommunityScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("Community")));
  }
}
