import 'package:flutter/material.dart';

class CommunityScreens extends StatelessWidget {
const CommunityScreens({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
 return Scaffold(
      body: Center(child: Text("Community"),),
    );   
  }
}